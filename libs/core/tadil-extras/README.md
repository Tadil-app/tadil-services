# tadil-extras

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tadil-extras` to build the library.

## Running unit tests

Run `nx test tadil-extras` to execute the unit tests via [Jest](https://jestjs.io).
