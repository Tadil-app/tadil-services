# tadil-tailor

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tadil-tailor` to build the library.

## Running unit tests

Run `nx test tadil-tailor` to execute the unit tests via [Jest](https://jestjs.io).
