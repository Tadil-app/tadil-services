# tadil-alterations

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tadil-alterations` to build the library.

## Running unit tests

Run `nx test tadil-alterations` to execute the unit tests via [Jest](https://jestjs.io).
