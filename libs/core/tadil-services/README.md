# tadil-services

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tadil-services` to build the library.

## Running unit tests

Run `nx test tadil-services` to execute the unit tests via [Jest](https://jestjs.io).
