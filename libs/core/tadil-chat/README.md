# tadil-chat

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tadil-chat` to build the library.

## Running unit tests

Run `nx test tadil-chat` to execute the unit tests via [Jest](https://jestjs.io).
