# tadil-users

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tadil-users` to build the library.

## Running unit tests

Run `nx test tadil-users` to execute the unit tests via [Jest](https://jestjs.io).
